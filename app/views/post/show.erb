<h1><%= @post.id%>번째 글</h1>
<div class="alert alert-info" role="alert">
<p>작성자 : <%= @post.user_email%></p>
<p>제목 : <%= @post.title%><br></p>
<p>내용 : <%= @post.content%></p>
</div>
<!-- 댓글 form -->
<h3>댓글</h3>
<form action = '/post/create_comment'>
    <input type = 'hidden' name = 'post_id' value = '<%= @post.id%>'>
    <input type = 'text' name = 'comment'>
    <input type = 'submit' value = '등록'>
</form><br>
<% @comments.each do |comment|%>
<form>
  <div class="form-group">
    <input type="text" class="form-control" id="formGroupExampleInput" placeholder="Example input" value = '<%= comment.content%>'>
  </div>
</form>
<%end%>
<a href='/post/modify/<%= @post.id%>'>수정</a>
<a href='/post/destroy/<%= @post.id%>'>삭제</a>
<a href='/'>목록으로</a>
